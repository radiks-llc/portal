---
import LoginPage from "@ui/forms/LoginPage";
import Enterprise from "@layouts/Enterprise.astro";

import { getUser } from "@astro-auth/core";
const user = getUser({ client: Astro });

import Profile from "./profile.astro";
---

<Enterprise showLogin={false} title="RadiKS Solutions">
  {user ? <Profile /> : <LoginPage client:only="react" />}
</Enterprise>
